<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_ledger">

    <fragment
        android:id="@+id/navigation_ledger"
        android:name="com.xcodeblocks.ledger_delivery.ui.ledger.LedgerFragment"
        android:label="@string/title_ledger"
        tools:layout="@layout/fragment_ledger" />

    <fragment
        android:id="@+id/navigation_delivery"
        android:name="com.xcodeblocks.ledger_delivery.ui.delivery.DeliveryFragment"
        android:label="@string/title_delivery"
        tools:layout="@layout/fragment_delivery" />

    <fragment
        android:id="@+id/navigation_settings"
        android:name="com.xcodeblocks.ledger_delivery.ui.settings.SettingsFragment"
        android:label="@string/title_settings"
        tools:layout="@layout/fragment_settings" />
</navigation>
